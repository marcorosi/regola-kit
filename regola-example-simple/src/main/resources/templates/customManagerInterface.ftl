package ${service_package};

import it.kion.regola.service.GenericManager;

import ${model_class};
import ${id_class};

public interface ${service_interface_name} extends GenericManager<${model_name},${id_name}>
{

}